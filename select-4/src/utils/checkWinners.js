// Author: Kelly

// import winningNumber

function checkWinner(winningNumber) {
    // GET route to get tickets, from all users, for today's date

    // Loop through array of tickets
        // Compare ticket to winningNumber
        // If there is a match, keep track of winning ticket and user
    
    // Return array of winners (will usually be empty)
}

